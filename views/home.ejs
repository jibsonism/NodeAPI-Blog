<!DOCTYPE html>
<html>
<head>    
    <style>
    body {
        font-family: Arial, sans-serif;
        margin: 0;
        padding: 0;
        background-color: #f4f4f4;
    }

    header {
        background-color: #333;
        color: #fff;
        padding: 10px;
        text-align: center;
    }

    section {
        max-width: 800px;
        margin: 20px auto;
        padding: 20px;
        background-color: #fff;
        box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
    }

    footer {
        background-color: #333;
        color: #fff;
        text-align: center;
        padding: 10px;
        position: fixed;
        bottom: 0;
        width: 100%;
    }
</style>
</head>    
<body>
    <section>
        <% blogs.forEach(blog => { %>
            <article>
                <a href="<%= blog.url %>"></a><h2><%= blog.title %></h2></a>
                    <p>Published on <time datetime="<%= blog.date %>"><%= blog.createdAt %></time>.</p>
                    <p><%= blog.snippet %></p>
                    <p><%= blog.body %></p>
            </article>
        <% }) %>
        
    <!-- You can add more articles here for additional blog posts -->

</section>
<script src="tut.js">
    
</script>

</body>
</html>